# Customer-Support-Agent
An automated customer support pipeline that filters, analyzes, and responds to incoming emails using Retrieval-Augmented Generation (RAG). This workflow ensures that only genuine support requests are processed by the AI, saving costs and improving response accuracy.
